package model.fields;

import model.fields.Field;

public class Jail extends Field {
    public Jail(String name, int index, String color, char type) {
        super(name, index, color, type);
    }
}
