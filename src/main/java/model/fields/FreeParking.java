package model.fields;

import model.fields.Field;

public class FreeParking extends Field {
   public FreeParking(String name, int index, String color, char type) {
        super(name, index, color, type);
    }
}
